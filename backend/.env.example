# Copy to backend/.env and adjust. Matches docker-compose.yml defaults.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=df_toolkit
DB_USER=df
DB_PASSWORD=df

# Optional: overrides the DB_* pieces above for Alembic (sync driver).
# DATABASE_URL=postgresql+psycopg2://df:df@localhost:5432/df_toolkit
